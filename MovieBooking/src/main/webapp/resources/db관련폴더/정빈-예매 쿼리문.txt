남은거


// 조건 1 영화선택시 지점 + 날짜 가져오기

SELECT
	SCREEN_SCHEDULE.mov_code as movCode,
	(SELECT MOVIE.mov_korName FROM MOVIE WHERE MOVIE.mov_code=SCREEN_SCHEDULE.mov_code) as movKorName,
	SCREEN_SCHEDULE.brc_code as brcCode,
	(SELECT BRANCH.brc_name FROM BRANCH WHERE BRANCH.brc_code = SCREEN_SCHEDULE.brc_code) as brcName,
	SCREEN_SCHEDULE.scs_date as scsDate
FROM
	SCREEN_SCHEDULE
WHERE
	SCREEN_SCHEDULE.mov_code=2100001
GROUP BY
	brcCode

	
SELECT
	SCREEN_SCHEDULE.mov_code as movCode,
	(SELECT MOVIE.mov_korName FROM MOVIE WHERE MOVIE.mov_code=SCREEN_SCHEDULE.mov_code) as movKorName,
	SCREEN_SCHEDULE.brc_code as brcCode,
	(SELECT BRANCH.brc_name FROM BRANCH WHERE BRANCH.brc_code = SCREEN_SCHEDULE.brc_code) as brcName,
	SCREEN_SCHEDULE.scs_date as scsDate
FROM
	SCREEN_SCHEDULE
WHERE
	SCREEN_SCHEDULE.mov_code=2100001
GROUP BY
	scsDate




// 조건 2 지점선택시 영화 + 날짜 가져오기

SELECT
	SCREEN_SCHEDULE.mov_code as movCode,
	(SELECT MOVIE.mov_korName FROM MOVIE WHERE MOVIE.mov_code=SCREEN_SCHEDULE.mov_code) as movKorName,
	SCREEN_SCHEDULE.brc_code as brcCode,
	(SELECT BRANCH.brc_name FROM BRANCH WHERE BRANCH.brc_code = SCREEN_SCHEDULE.brc_code) as brcName,
	SCREEN_SCHEDULE.scs_date as scsDate
FROM
	SCREEN_SCHEDULE
WHERE
	SCREEN_SCHEDULE.brc_Code=41101
GROUP BY
	movCode

SELECT
	SCREEN_SCHEDULE.mov_code as movCode,
	(SELECT MOVIE.mov_korName FROM MOVIE WHERE MOVIE.mov_code=SCREEN_SCHEDULE.mov_code) as movKorName,
	SCREEN_SCHEDULE.brc_code as brcCode,
	(SELECT BRANCH.brc_name FROM BRANCH WHERE BRANCH.brc_code = SCREEN_SCHEDULE.brc_code) as brcName,
	SCREEN_SCHEDULE.scs_date as scsDate
FROM
	SCREEN_SCHEDULE
WHERE
	SCREEN_SCHEDULE.brc_Code=41101
GROUP BY
	scsDate


// 조건 3 날짜 선택시 영화 + 지점 가져오기

SELECT
	SCREEN_SCHEDULE.mov_code as movCode,
	(SELECT MOVIE.mov_korName FROM MOVIE WHERE MOVIE.mov_code=SCREEN_SCHEDULE.mov_code) as movKorName,
	SCREEN_SCHEDULE.brc_code as brcCode,
	(SELECT BRANCH.brc_name FROM BRANCH WHERE BRANCH.brc_code = SCREEN_SCHEDULE.brc_code) as brcName,
	SCREEN_SCHEDULE.scs_date as scsDate
FROM
	SCREEN_SCHEDULE
WHERE
	SCREEN_SCHEDULE.scs_Date='2018-03-01'
GROUP BY
	movCode
	
SELECT
	SCREEN_SCHEDULE.mov_code as movCode,
	(SELECT MOVIE.mov_korName FROM MOVIE WHERE MOVIE.mov_code=SCREEN_SCHEDULE.mov_code) as movKorName,
	SCREEN_SCHEDULE.brc_code as brcCode,
	(SELECT BRANCH.brc_name FROM BRANCH WHERE BRANCH.brc_code = SCREEN_SCHEDULE.brc_code) as brcName,
	SCREEN_SCHEDULE.scs_date as scsDate
FROM
	SCREEN_SCHEDULE
WHERE
	SCREEN_SCHEDULE.scs_Date='2018-03-01'
GROUP BY
	brcCode


// 조건1-1 영화 + 지점 선택시 날짜 가져오기

// 조건 1-2 영화 + 날짜 선택시 지점 가져오기


// 조건 2-1 지점+영화 선택시 날짜 가져오기

// 조건 2- 2 지점 + 날짜 선택시 영화 가져오기



// 조건 3-1 날짜 + 영화 선택시 지점 가져오기

// 조건 3-2 지점 + 날짜 선택시 영화 가져오기





예매 ajax

댓글

좋아요

극장

이벤트

